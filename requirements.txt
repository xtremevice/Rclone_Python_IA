# Rclone Python IA – Python dependencies
# Install with: pip install -r requirements.txt

# System tray icon support (cross-platform, used by src/gui/tray_icon.py)
pystray>=0.19.5

# Image manipulation (icon generation, tray icon)
Pillow>=10.0.0

# PyQt5 UI toolkit (used by core/, ui/, app/ modules)
PyQt5>=5.15.0
PyQt5-sip>=12.8.0

# Fernet symmetric encryption used by the per-service file-scan database
# (src/db/file_scan_db.py).  AES-128-CBC + HMAC-SHA256 at rest.
cryptography>=41.0.0

# Build tool (produces single-file executables)
# pyinstaller>=6.0.0

# Test runner
pytest>=7.0.0
